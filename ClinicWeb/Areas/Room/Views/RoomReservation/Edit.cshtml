@model ClinicWeb.Areas.Room.Models.AppointmentRoomSchedule

<form asp-action="Edit">
    <div class="form-group">
        <label asp-for="EmpId" class="control-label"></label>
        <select asp-for="EmpId" class="form-control" asp-items="@(new SelectList(ViewBag.Employees, "EmpId", "Name"))"></select>
        <span asp-validation-for="EmpId" class="text-danger"></span>
    </div>
    <!-- Add other form fields for MemberId, RoomId, and other properties -->
    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>
</form>
