@model IEnumerable<ClinicWeb.Areas.Drugs.Models.PharmacyTTypeList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FTypeCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FTypeCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FIdType">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FIdType">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FIdType">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
